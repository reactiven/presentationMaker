const SlideElement = {
    type: 'texBox',
    width: 1000,
    height: 1000,
    xPos: 200,
    yPos: 300,
    elementId: 1,
}

function moveElement(state, newX, newY): state {}
function resizeElement(state, newWidth, newHeight): state {}