const SlideElement = {
    type: 'texBox',
    dataElement: TextBox,
    width: 1000,
    height: 1000,
    xPos: 200,
    yPos: 300,
    elementId: 1,
}

function moveElement(state, newX, newY){
    return state;
}
function resizeElement(state, newWidth, newHeight){
    return state;
}